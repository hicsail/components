import { ThemeOptions } from '@mui/material';

export const baseTheme: Partial<ThemeOptions> = {
  shape: {
    borderRadius: 10
  },
  typography: {
    button: {
      textTransform: 'none'
    }
  }
};
